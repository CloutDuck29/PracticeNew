<Window x:Class="PracticaProj.Captcha"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PracticaProj"
        mc:Ignorable="d"
        Title="Captcha" Height="225" Width="300">
    <Grid Style="{StaticResource MainContainer}">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <UniformGrid VerticalAlignment="Bottom" Grid.Row="0">
            <Label Style="{StaticResource Label}">Input</Label>
            <TextBox></TextBox>
        </UniformGrid>
        <Border VerticalAlignment="Top" Grid.Row="1" Padding="5" CornerRadius="10" Background="DarkGray">
            <UniformGrid>
                <Label VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontWeight="Bold" FontSize="20">
                    Captcha 10 symbols
                </Label>
            </UniformGrid>
        </Border>
    </Grid>
</Window>
